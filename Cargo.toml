[package]
name = "moka-expired-upsert-repro"
version = "0.1.0"
edition = "2021"

[dependencies]
moka = { version = "=0.12.15", features = ["future"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
